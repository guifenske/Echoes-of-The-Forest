// Evento apenas para debug


// Desenhar ele mesmo, ava
draw_self()


if(canshow == 1){
	draw_set_color(c_blue)
	draw_text(x, y - 180, "pulos restantes: " + string_format(pulos, 0, 0))
}

	draw_set_color(c_blue)
	draw_text(x, y - 200, "velocidade: " + string_format(hsp, 0, 0))

